{
  "name": "tooltip-layout-tests",
  "private": true,
  "type": "module",
  "description": "Headless layout-invariant guard for the shared cursor tooltip (src/plotting/_scaffold).",
  "scripts": {
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0"
  }
}
